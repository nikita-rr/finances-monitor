# Telegram Finance Monitor Bot 💰

Telegram бот для отслеживания месячного бюджета в чате.

## Функциональность

✅ Установка месячного бюджета  
✅ Отслеживание дневного лимита (бюджет / 30 дней)  
✅ Добавление расходов и доходов в формате: `-300 обед -200 такси`  
✅ Автоматическое обновление закрепленного сообщения со статусом  
✅ Просмотр статуса и истории транзакций  
✅ Сохранение данных в JSON файл  

## Требования

- Node.js 16+
- Telegram Bot Token (получить можно у [@BotFather](https://t.me/botfather))

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repo-url>
cd tg-finance-monitor
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env`:
```bash
cp .env.example .env
```

4. Добавьте ваш BOT_TOKEN в `.env`:
```
BOT_TOKEN=your_telegram_bot_token_here
```

## Запуск

**Разработка (с перезагрузкой):**
```bash
npm run dev
```

**Продакшн:**
```bash
npm run build
npm start
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Справка по боту |
| `/help` | Подробная справка |
| `/setbudget <сумма>` | Установить месячный бюджет |
| `/status` | Показать текущий статус |
| `/pin` | Закрепить сообщение со статусом в чате |
| `/reset` | Сбросить и начать новый месяц |

## Использование

### Установка бюджета
```
/setbudget 30000
```

### Добавление транзакций
Просто напишите в чат сумму с описанием:

**Отдельные транзакции:**
```
-300 обед
-200 такси
+500 зарплата
```

**Несколько за раз:**
```
-300 обед -200 сигареты +100 возврат
```

Отрицательные числа = расходы  
Положительные числа = доходы/пополнения

### Просмотр статуса
```
/status
```

### Закрепить статус
```
/pin
```

Статус будет закреплен в чате и будет автоматически обновляться при добавлении новых транзакций.

## Структура данных

Данные сохраняются в файл `data.json`:

```json
{
  "-1001234567890": {
    "monthlyBudget": 30000,
    "currentSpent": 500,
    "transactions": [
      {
        "amount": -300,
        "description": "обед",
        "date": "2024-01-15T10:30:00.000Z",
        "userId": 123456,
        "userName": "John"
      }
    ],
    "pinnedMessageId": 12345,
    "createdDate": "2024-01-01T00:00:00.000Z"
  }
}
```

## Особенности

- 📊 Отслеживание месячного и дневного бюджета
- 📈 Автоматический расчет дневного лимита
- 💾 Сохранение данных между перезагрузками
- 📌 Закрепленное сообщение обновляется автоматически
- 🚨 Оповещение при превышении лимитов
- 📱 Интуитивный интерфейс

## Лицензия

ISC
