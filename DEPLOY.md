# Как сделать приложение доступным из интернета

## Вариант 1: LocalTunnel (Самый быстрый)

LocalTunnel предоставляет публичный URL без регистрации.

### Шаги:

1. Запустите сервер в одном терминале:
```bash
npm run dev:miniapp
```

2. В другом терминале запустите туннель:
```bash
npm run tunnel:local
```

3. Скопируйте URL (например, `https://your-name.loca.lt`)

4. При первом открытии нажмите кнопку для подтверждения

5. Используйте этот URL в BotFather

**Плюсы:** Быстро, не требует регистрации  
**Минусы:** URL меняется при перезапуске

---

## Вариант 2: Cloudflare Tunnel (Бесплатно, надёжно)

Cloudflare Tunnel создаёт безопасный туннель без открытия портов.

### Установка:

```bash
# macOS
brew install cloudflare/cloudflare/cloudflared

# Или скачать с https://github.com/cloudflare/cloudflared/releases
```

### Использование:

```bash
# 1. Запустите сервер
npm run dev:miniapp

# 2. В другом терминале создайте туннель
cloudflared tunnel --url http://localhost:3000
```

Скопируйте HTTPS URL и используйте в BotFather.

**Плюсы:** Быстрый, надёжный, бесплатный  
**Минусы:** Требует установки утилиты

---

## Вариант 3: ngrok (Если установлен)

Если установите ngrok через Homebrew:

```bash
brew install ngrok

# Авторизация (токен с dashboard.ngrok.com)
ngrok config add-authtoken YOUR_TOKEN

# Запуск
npm run dev:miniapp    # в одном терминале
npm run tunnel:ngrok   # в другом терминале
```

**Плюсы:** Популярный, стабильный  
**Минусы:** Требует регистрации и установки

---

## Вариант 4: Деплой на сервер (Production)

### Railway.app (Рекомендуется)

1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект
3. Подключите GitHub репозиторий или загрузите код
4. Railway автоматически определит Node.js приложение
5. Добавьте переменную окружения `BOT_TOKEN`
6. Деплой произойдёт автоматически
7. Получите публичный URL

### Render.com

1. Зарегистрируйтесь на https://render.com
2. Создайте новый Web Service
3. Подключите репозиторий
4. Build Command: `npm install && npm run build`
5. Start Command: `npm start`
6. Добавьте `BOT_TOKEN` в Environment
7. Получите публичный URL

### VPS (Ваш сервер)

Если у вас есть VPS с Docker:

```bash
# 1. Скопируйте код на сервер
scp -r . user@your-server:/path/to/app

# 2. На сервере
cd /path/to/app
echo "BOT_TOKEN=your_token" > .env
echo "{}" > data.json

# 3. Запустите через Docker
docker compose build
docker compose up -d

# 4. Настройте nginx как reverse proxy
```

Nginx конфиг:
```nginx
server {
    listen 80;
    server_name yourdomain.com;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

Для HTTPS используйте Certbot:
```bash
sudo certbot --nginx -d yourdomain.com
```

---

## Рекомендация

**Для разработки:** LocalTunnel или Cloudflare Tunnel  
**Для production:** Railway.app или Render.com

Команды для быстрого старта:

```bash
# Терминал 1
npm run dev:miniapp

# Терминал 2 (выберите один)
npm run tunnel:local                              # LocalTunnel
cloudflared tunnel --url http://localhost:3000    # Cloudflare
```

После получения URL добавьте его в BotFather:
1. Откройте @BotFather
2. /mybots → ваш бот → Bot Settings → Menu Button
3. Configure menu button
4. Вставьте полученный HTTPS URL
5. Введите текст кнопки: "💰 Открыть приложение"
